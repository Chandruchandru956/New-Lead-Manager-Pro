kind = "web"
previewPath = "/"
title = "Lead Tracker CRM"
version = "1.0.0"
id = "artifacts/lead-tracker"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23428

[services.development]
run = "pnpm --filter @workspace/lead-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lead-tracker", "run", "build" ]
serve = "static"
publicDir = "artifacts/lead-tracker/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23428"
BASE_PATH = "/"
